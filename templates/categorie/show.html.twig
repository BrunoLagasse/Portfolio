{% extends 'base.html.twig' %}

{% block title %}Technologie{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center">Technologie :<span style="color: #F78F96;">&nbsp;{{categorie.name}}</span></h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary"href="{{ path('categorie_index') }}">Retour à la liste</a>

    <a class="btn btn-success"href="{{ path('categorie_edit', {'id': categorie.id}) }}">Modifier</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
